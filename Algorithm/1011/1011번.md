# 1011번

```c
#include <stdio.h>
int distance;
int min=9999;
int func(int n_move,int count,int sum)
{
    if(n_move<=0 || sum>distance)
    {
        return 0;
    }
    if(sum==distance)
    {
        if(min>count)
        {
            min=count;
            return 0;
        }
    }
    func(n_move+1,count+1,sum+n_move+1);
    func(n_move,count+1,sum+n_move);
    func(n_move-1,count+1,sum+n_move-1);
    return 0;
}
int main() {
    int s, e;
    int n;
    scanf("%d", &n);
    for (int i = 0; i < n; ++i) {
        scanf("%d %d", &s, &e);
        distance = e - s - 2;
        func(1, 0, 0);
        printf("%d",min+2);
        min=9999;
    }
}
```

