# 1157번

```c
#include <stdio.h>
#include <ctype.h>
#include <string.h>
int main()
{
    char a[1000];
    int b[30]={0,};
    int max=0,ch=0;
    scanf("%s",a);
    for (int i = 0; i < strlen(a); ++i) {
        b[towupper(a[i])-65]++;
    }
    for (int i = 0; i < 30; ++i) {
        if(max<b[i])
        {
            max=b[i];
            ch=i;
        }
    }
    printf("%c",ch+65);
}
```

